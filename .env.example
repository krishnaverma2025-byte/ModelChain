# Server/deployment configuration only; never expose these through VITE_*.
SEPOLIA_RPC_URL=
SEPOLIA_PRIVATE_KEY=
# Supply PINATA_JWT to backend, not the browser.
PINATA_JWT=
# Preflight: MONTAI_DEPLOY_DRY_RUN=1 npx hardhat run scripts/deploy.ts --network sepolia
# No deployment approval is encoded in this file.
